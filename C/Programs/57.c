#include<stdio.h>
int main(){
	
	// two strings equal or not?
	// strcmp(str1, str2) = 0
	// char by char - user code
	// concatenation
	// name = first name + last name
	// name = strcat(fn,sn)
	// user code; name = fn
	// Length of a string
	// strlen(Str); 
	// user code - \0 - null 
    // string reverse
    
    // string is polyndrome
    
    // substring = get a substring = (2,5)
    
    // substr("vit university", "vit") = check substring is available or not
    
    // user code
	return 0;
}

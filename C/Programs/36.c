#include<stdio.h>
int main(){
	
	int n=0, pos=0, neg=0, tot=0, count=0, avg;
	printf("Enter an integer, the input ends if it is 0: ");
	
	while(n!=0){
		scanf()
		if(n>0){
			pos++;
			n++;
		}
			
		else if(n<0){
			neg++;
			n++;
		}
	}
	tot = pos+neg;
	printf("The number of positives is %d", pos);
	printf("The number of negatives is %d", neg);
	printf("The total is %d", tot);	
	
	return 0;
}

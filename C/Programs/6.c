#include<stdio.h>

int main(){
	// Arithmetic, Increment/decrement, Relational(<, >, <=, >=, ==, !=)
	// Logical(&& , ||, !)
	
	// Bit-wise operators(&, |, XOR)- Structures
	// (), [], ., ->
	
	// Operator precedence(priority) and Associativity
	
	int a;
	int b=4, c=8, d=2, e=4, f=2;
	
	a=b+c/d+e*f; // *, /, +
	
	printf("%d", a);
	
}
